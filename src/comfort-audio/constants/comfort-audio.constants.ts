/** S3 key prefix (folder) under the assets bucket for uploaded comfort-audio tracks. */
export const COMFORT_AUDIO_LIBRARY_S3_PREFIX = 'comfort-audio-library';

/** Max upload size for a comfort-audio file (20 MB). */
export const COMFORT_AUDIO_MAX_FILE_SIZE_BYTES = 20 * 1024 * 1024;
