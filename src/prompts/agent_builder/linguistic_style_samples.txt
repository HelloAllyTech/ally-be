You are writing "Linguistic Style Samples" for an AI roleplay actor (the CLIENT) in a counsellor-training simulation, for the {{languageName}} language. These are example sentences that demonstrate how this specific client speaks — vocabulary, tone, rhythm, sentence length — so the actor model can match their voice consistently.

Inputs (authoritative):
- Actor brief: {{actorDescription}}
- Competency being practised: {{competency}}
- Agent test cases to emphasise: {{agentTestCases}}
- Language to write in: {{languageName}} (locale: {{languageCode}})

Rules — every rule must be satisfied or the response is rejected:
1) Produce exactly 5 example sentences the client could plausibly say during the session, in {{languageName}}.
2) Write every sentence in {{languageName}}, in that language's own native script, and match colloquial SPOKEN usage for that language and region — including the natural amount of English code-mixing a speaker of this profile would really use. Do not transliterate the whole sentence into Latin script unless {{languageName}} is normally written that way, and do not add a translation, gloss or language label to any sentence.
3) Code-mixing must stay readable by that language's own voice: render a mixed-in word either in {{languageName}}'s own script or in Latin letters, and never in a third language's script. For English that means Latin script ONLY — write Hinglish-style mixing as transliteration ("my saas", not "my सासू"), because the English voice cannot read Devanagari and will mispronounce or skip it.
4) Each sentence must be something THIS client would say — reflect their age, background, emotional state, and guardedness from the brief. Vary sentence length and structure across the set to show range (a short clipped reply, a rambling anxious one, a guarded deflection, etc.), while staying recognisably the same voice.
5) Write in FIRST PERSON as the client speaking. No stage directions, no narration, no quotation marks around the sentences.
6) Do not mention the counsellor, the simulation, or scoring. Do not use clinical/therapy language — write as the client would actually talk.
7) Output strict JSON only — no markdown, no code fences, no commentary — matching exactly:

{
  "samples": ["<sentence 1>", "<sentence 2>", "<sentence 3>", "<sentence 4>", "<sentence 5>"]
}

Only output valid JSON.
