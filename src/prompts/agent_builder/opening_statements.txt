You are writing the "Opening Dialogues" for an AI roleplay actor used in counsellor training, for the {{languageName}} language. These are the first lines the CLIENT might say to open the session, before the counsellor has said anything. The platform picks one to start the conversation.

Inputs (authoritative):
- Actor brief: {{actorDescription}}
- Competency being practised: {{competency}}
- Agent test cases to emphasise: {{agentTestCases}}
- Language to write in: {{languageName}} (locale: {{languageCode}})

Rules — every rule must be satisfied:
1) Produce 3–5 alternative opening lines, each a complete, natural utterance the client could say to start the session — a greeting, a hesitant opener, a way of naming why they're there, etc. Vary the register/approach across the lines (e.g. one guarded, one more forthcoming).
2) Write in FIRST PERSON as the client speaking aloud. Keep each line short and natural for spoken dialogue (roughly 1–3 sentences).
3) Write every line in {{languageName}}, in that language's own native script, and match colloquial SPOKEN usage for that language and region — including the natural amount of English code-mixing a speaker of this profile would really use. Do not transliterate the whole line into Latin script unless {{languageName}} is normally written that way, and do not add a translation, gloss or language label to any line.
4) Code-mixing must stay readable by that language's own voice: render a mixed-in word either in {{languageName}}'s own script or in Latin letters, and never in a third language's script. For English that means Latin script ONLY — write Hinglish-style mixing as transliteration ("my saas", not "my सासू"), because the English voice cannot read Devanagari and will mispronounce or skip it.
5) Reflect the client's emotional state and guardedness from the brief; do not resolve or summarise their whole situation in the opener — this is just how they'd break the silence.
6) Do NOT include speaker labels, quotation marks, numbering, or bullet markers — just the line itself.
7) Output PLAIN TEXT only, ONE opening line per text line, nothing else — no markdown, no headings, no JSON, no code fences, no preamble or sign-off.

Write the opening dialogue lines now.
