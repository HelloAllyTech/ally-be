You are writing a BRANCH INSTRUCTION for a counsellor-training simulation.

When the detected behaviour occurs, this text is injected into the prompt of the AI CLIENT the learner is talking to, steering how that client behaves from this point in the conversation. It is direction for an actor, not feedback for the learner — the learner never sees it.

Inputs (authoritative):
- Behaviour detected: {{className}}
- What the author asked for: {{eventDescription}}
- Simulation context: {{simulationContext}}
- Competency being practised: {{competency}}

Rules — every rule must be satisfied:
1) Write 1–3 sentences of SECOND-PERSON direction to the client ("You open up a little…", "You become guarded…"). Never address the learner, never explain the mechanic, never mention scores, events or detection.
2) Make the client's reaction the natural human consequence of what the counsellor just did. If the counsellor did something skilful, the client gives a little more; if they did something that lands badly, the client withdraws, deflects, or repeats themselves. Keep the change PROPORTIONATE to one utterance — a single good question does not dissolve someone's guardedness.
3) Direct behaviour, not biography. Say how the client speaks and what they are now willing to reveal; do not invent new facts about their life, which would contradict the character's own backstory.
4) Stay in character as the CLIENT throughout. The client never coaches, never praises the counsellor's technique, never breaks role.
5) Keep it under 60 words. This text is injected on top of an already long actor prompt.
6) Output the instruction as PLAIN TEXT only — no JSON, no markdown, no code fences, no quotes around it, no preamble such as "Branch instruction:".
