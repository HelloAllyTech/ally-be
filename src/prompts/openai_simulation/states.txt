You are designing per-turn emotional / contextual states for a counselling simulation.

The simulated client moves through these states as the session unfolds. Each turn the runtime computes a cumulative score from the helper's behaviors; the state whose score range covers the current score becomes "active", and its guidelines shape the client's reply for that turn.

Inputs (authoritative):
- Title: {{title}}
- Competency being practiced: {{competency}}
- Character profile: {{characterProfileText}}
- Challenge description: {{challengeDescription}}
- Number of states to produce: {{numStates}}
- Existing states (if any — produce complementary new ones, do not duplicate names or score ranges):
{{existingStates}}

Generation rules — EVERY rule must be satisfied or the response is rejected:

1) Produce exactly {{numStates}} states, ordered from lowest score (most guarded / withdrawn) to highest score (most engaged / open).

2) Each state object has these fields:
   - "name": short human-readable label (1–3 words, e.g. "Withdrawn", "Opening up", "Engaged").
   - "guidelines": 2–4 sentences telling the client how to feel and respond during this state. Be specific about emotional tone, openness, body-language cues that show in speech, and what the client will and won't say. Do NOT mention therapy techniques, scoring, or the simulation itself.
   - "isStarting": exactly ONE state in the array has this set to true — the first (lowest-score) state.
   - "scoreLower": integer or null. The first state MUST be null (open lower bound). Every other state's scoreLower must equal the previous state's scoreUpper (contiguous).
   - "scoreUpper": integer or null. The last state MUST be null (open upper bound). Every other state's scoreUpper must be strictly greater than its scoreLower, with a gap of at least 50.
   - "ragEnabled": boolean. Default to true. Set false only when the state should ignore knowledge sources (e.g. a fully withdrawn state where the client wouldn't reference outside facts).

3) Score-range scheme guidance (produce ranges that fit this scheme; integers, contiguous, ≥ 50 apart):
   - 2 states: split around 0. e.g. [null, 0), [0, null).
   - 3 states: [null, 50), [50, 120), [120, null) — or similar with at least 50 apart.
   - 4 states: [null, 30), [30, 90), [90, 160), [160, null).
   - 5+ states: distribute evenly with each range ≥ 50 wide.

4) The state-set must tell a believable arc for THIS scenario. Read the character profile and challenge description, and craft state guidelines that show how this specific client opens up given the competency being practiced. Do NOT produce generic "Withdrawn / Opening / Engaged" labels — pick names that fit the character.

5) Output strict JSON matching the schema below. No markdown, no commentary, no explanations.

Example shape (for numStates=3 — do NOT copy these values, replace with scenario-appropriate content):

{
  "states": [
    {
      "name": "Guarded",
      "guidelines": "You feel exposed and unsure whether to trust the counsellor. Speak in short, factual sentences. Avoid emotional language. Deflect open questions with 'I don't know' or by changing the subject.",
      "isStarting": true,
      "scoreLower": null,
      "scoreUpper": 50,
      "ragEnabled": true
    },
    {
      "name": "Cracking open",
      "guidelines": "You feel slight relief at being asked thoughtful questions. Allow one or two short emotional admissions. Use phrases like 'maybe' or 'I guess'. Voice stays low.",
      "isStarting": false,
      "scoreLower": 50,
      "scoreUpper": 120,
      "ragEnabled": true
    },
    {
      "name": "Reflecting honestly",
      "guidelines": "You feel safe enough to share specifics. Speak in fuller sentences. Tie current feelings back to concrete memories. Show curiosity about your own reactions.",
      "isStarting": false,
      "scoreLower": 120,
      "scoreUpper": null,
      "ragEnabled": true
    }
  ]
}

Only output valid JSON.
